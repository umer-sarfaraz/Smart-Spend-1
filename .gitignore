node_modules/
dist/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Local tooling & personal test images
.claude/
receiptsample.jpeg

# Private working files — this repo is PUBLIC, keep these out of it
Chat_Transcripts/
CLAUDE_MEMORY.md
Backups/

# Native app lives in its own PRIVATE repo (github.com/umer-sarfaraz/smartspend-native) — never in this public one
Mobile-App/
_stage_native.tar.gz
